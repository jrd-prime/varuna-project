<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/constLayDate"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:background="@color/colorLightGrey"
    app:layout_constraintTop_toBottomOf="@+id/fr">


        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_chevron_left"
            app:layout_constraintBottom_toBottomOf="@+id/tvDateScroll"
            app:layout_constraintEnd_toStartOf="@+id/tvDateScroll"
            app:layout_constraintTop_toTopOf="@+id/tvDateScroll"
            android:contentDescription="@string/minusDay" />

        <TextView
            android:id="@+id/tvDateScroll"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:text="@string/textMock"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_chevron_right"
            app:layout_constraintBottom_toBottomOf="@+id/tvDateScroll"
            app:layout_constraintStart_toEndOf="@+id/tvDateScroll"
            app:layout_constraintTop_toTopOf="@+id/tvDateScroll"
            android:contentDescription="@string/plusDay" />

</androidx.constraintlayout.widget.ConstraintLayout>